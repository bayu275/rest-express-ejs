<!DOCTYPE html>
<html lang="en">
    <%- include('../partials/head.ejs') %>
    <body>
        <div class="container">
            <div class="row justify-content-center mt-4">
                <h1 class="text-center">Details Comment</h1>
                <form
                    class="border p-3 col-md-6 rounded"
                    action="/comments/<%= comment.id %>?_method=PATCH"
                    method="post"
                >
                    <div>
                        <h6>username : <%= comment.username %></h6>
                    </div>
                    <div class="form-group mb-3">
                        <label for="floatingTextarea">comments :</label>
                        <textarea
                            class="form-control"
                            name="text"
                            placeholder="Leave a comment here"
                            id="floatingTextarea"
                            required
                            oninvalid="this.setCustomValidity('Comment cannot be empty')"
                            oninput="this.setCustomValidity('')"
                            disabled
                        >
<%= comment.text %></textarea
                        >
                    </div>
                    <a class="btn btn-primary" href="/comments/<%= comment.id %>/edit" role="button">Edit</a>
                    <a class="btn btn-secondary" href="/comments" role="button">Back to Comments</a>
                </form>
            </div>
        </div>
    </body>
</html>
